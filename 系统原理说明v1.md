# IMM-JPDA-EKF 多目标融合跟踪系统
## 原理说明文件

> **面向对象**：信号处理、控制理论、应用数学等不同背景的读者  
> **核心算法来源**：Bar-Shalom《Multitarget-Multisensor Tracking》(2001)；Blackman & Popoli《Design and Analysis of Modern Tracking Systems》(1999)

---

## 目录

1. [问题背景与系统目标](#1-问题背景与系统目标)
2. [整体架构与数据流](#2-整体架构与数据流)
3. [目标状态描述](#3-目标状态描述)
4. [Step 1：IMM 预测](#4-step-1imm-预测)
5. [Step 2：量测门控](#5-step-2量测门控)
6. [Step 3：JPDA 数据关联](#6-step-3jpda-数据关联)
7. [Step 4：PDA 状态更新](#7-step-4pda-状态更新)
8. [Step 5~7：轨迹管理](#8-step-57轨迹管理)
9. [参数选择与调参指南](#9-参数选择与调参指南)
10. [公式索引与代码对照表](#10-公式索引与代码对照表)

---

## 1. 问题背景与系统目标

### 1.1 我们要解决什么问题？

想象在城市低空有多架无人机同时飞行。地面部署了两种传感器：

- **雷达**（Radar）：位置精度高（误差约 1 米），但受建筑遮挡
- **声学麦克风阵列**（Acoustic）：通过无人机旋翼噪声定位，精度较低（误差约 8 米），但穿透性强

每帧（0.1 秒一帧），传感器会产生若干**探测点**（Detection）。这些探测点来源不明——可能来自真实无人机，也可能来自背景噪声（杂波，Clutter）。

**核心挑战**：

```
┌─ 哪个探测来自哪架无人机？（数据关联问题）
├─ 无人机在哪里、速度多大？（状态估计问题）
├─ 无人机是否在机动？（运动模式问题）
└─ 有几架无人机？目标是否已消失？（轨迹管理问题）
```

### 1.2 本系统的答案

采用经典三层结构：

| 层次 | 算法 | 解决什么 |
|---|---|---|
| 运动建模 | **IMM**（交互多模型） | 无人机可能匀速也可能机动 |
| 数据关联 | **JPDA**（联合概率数据关联） | 探测与目标的对应关系 |
| 状态估计 | **EKF**（扩展卡尔曼滤波） | 最优位置与速度估计 |
| 轨迹管理 | **M/N 逻辑 + 状态机** | 目标出现与消失的判断 |

---

## 2. 整体架构与数据流

### 2.1 每帧的处理流程（8 步严格顺序）

```
传感器探测（来自 Step2_HeteroDetections.mat）
          │
          ▼
┌─────────────────────────────────────────────────────────────┐
│  每帧循环（k = 1, 2, ..., N）                                │
│                                                             │
│  ① IMM 预测：对所有活跃轨迹推演下一位置                     │
│       ↓                                                     │
│  ② 量测门控：过滤掉明显不可能的探测                         │
│       ↓                                                     │
│  ③ JPDA 关联：计算每个探测来自每个目标的概率                 │
│       ↓                                                     │
│  ④ PDA 更新：用加权探测更新目标状态                         │
│       ↓                                                     │
│  ⑤ 轨迹确认：判断 Tentative 轨迹是否升级为 Confirmed        │
│       ↓                                                     │
│  ⑥ 轨迹诞生：未被任何轨迹覆盖的探测创建新轨迹               │
│       ↓                                                     │
│  ⑦ 轨迹删除：长时间未更新的轨迹标记为 Deleted               │
│       ↓                                                     │
│  ⑧ 输出当前帧所有轨迹状态                                   │
└─────────────────────────────────────────────────────────────┘
          │
          ▼
    性能评估 + 可视化
```

### 2.2 文件与模块对应关系

```
main_imm_jpda_ekf.m          ← 主循环，协调上述 8 步
├── IMM/
│   ├── imm_mix.m            ← ① 的前半部分：交互混合
│   ├── imm_predict.m        ← ① 的后半部分：预测
│   ├── imm_fuse.m           ← ① 的输出：融合估计
│   └── imm_update_mu.m      ← ④ 的一部分：更新模型概率
├── JPDA/
│   ├── jpda_gating.m        ← ② 门控
│   ├── jpda_cluster.m       ← ③ 聚类
│   ├── jpda_enumerate.m     ← ③ 枚举联合事件
│   ├── jpda_compute_beta.m  ← ③ 计算关联概率 β
│   └── jpda_run.m           ← ③ 的总调度
├── EKF/
│   └── ekf_pda_update.m     ← ④ PDA 状态更新
└── TrackManagement/
    ├── track_create.m       ← ⑥ 诞生新轨迹
    ├── track_imm_pda_update.m ← ④ 整合 IMM+PDA 的更新
    └── track_manage.m       ← ⑤⑥⑦ 轨迹生命周期管理
```

---

## 3. 目标状态描述

### 3.1 状态向量

每条轨迹用一个 **4 维向量**描述目标在某一时刻的运动状态：

$$\mathbf{x} = \begin{bmatrix} p_x \\ p_y \\ v_x \\ v_y \end{bmatrix}$$

其中 $p_x, p_y$ 是平面坐标（米），$v_x, v_y$ 是速度（米/秒）。

### 3.2 状态协方差矩阵

与状态向量配套，还有一个 **4×4 的协方差矩阵** $P$，表示我们对状态估计的不确定程度：

$$P = \begin{bmatrix}
\sigma_{p_x}^2 & \cdots & \cdots & \cdots \\
\vdots & \sigma_{p_y}^2 & \cdots & \cdots \\
\vdots & \vdots & \sigma_{v_x}^2 & \cdots \\
\vdots & \vdots & \vdots & \sigma_{v_y}^2
\end{bmatrix}$$

对角元素越大，说明对该分量的估计越不确定。$P$ 是正定对称矩阵。

### 3.3 量测向量

传感器只能观测到**位置**（不能直接测速度），因此量测向量为 2 维：

$$\mathbf{z} = \begin{bmatrix} z_x \\ z_y \end{bmatrix}$$

量测矩阵 $H$ 将状态映射到量测空间：

$$\mathbf{z} = H \mathbf{x} + \mathbf{w}, \quad H = \begin{bmatrix} 1 & 0 & 0 & 0 \\ 0 & 1 & 0 & 0 \end{bmatrix}$$

其中 $\mathbf{w} \sim \mathcal{N}(\mathbf{0}, R)$ 为量测噪声，$R$ 是 2×2 的量测噪声协方差矩阵。

**对应代码**（`config_tracker.m`）：

```matlab
cfg.H = [1 0 0 0;
         0 1 0 0];
cfg.R_radar    = diag([1.0^2, 1.0^2]);   % 雷达位置噪声 ~1m
cfg.R_acoustic = diag([8.0^2, 8.0^2]);   % 声学位置噪声 ~8m
```

---

## 4. Step 1：IMM 预测

### 4.1 为什么需要 IMM？

单一运动模型的卡尔曼滤波面临一个两难困境：

- **过程噪声小**（Q 小）：适合匀速飞行，但目标机动时 RMSE 剧增
- **过程噪声大**（Q 大）：适合机动，但匀速时估计抖动

**IMM（Interactive Multiple Model）的思路**：同时维护多个运动模型，用概率 $\mu_j$ 表示"当前最可能是哪个模型"，每帧根据量测自动调整。

### 4.2 两个运动模型

两个模型均采用**离散白噪声加速度（DWNA）模型**，状态转移矩阵相同：

$$F = \begin{bmatrix} 1 & 0 & \Delta t & 0 \\ 0 & 1 & 0 & \Delta t \\ 0 & 0 & 1 & 0 \\ 0 & 0 & 0 & 1 \end{bmatrix}$$

区别在于过程噪声协方差矩阵 $Q$（Bar-Shalom 2001, eq. 6.3.3-1）：

$$Q_j = q_j \begin{bmatrix}
\Delta t^3/3 & 0 & \Delta t^2/2 & 0 \\
0 & \Delta t^3/3 & 0 & \Delta t^2/2 \\
\Delta t^2/2 & 0 & \Delta t & 0 \\
0 & \Delta t^2/2 & 0 & \Delta t
\end{bmatrix}$$

| 模型 | 编号 | $q$ 值 | 物理含义 |
|---|---|---|---|
| CV（匀速） | j=1 | $q_1 = 0.5\ \text{m}^2/\text{s}^3$ | 加速度扰动小 |
| 高机动 | j=2 | $q_2 = 8.0\ \text{m}^2/\text{s}^3$ | 允许大加速度 |

**对应代码**（`imm_init_models.m`）：

```matlab
Q = q * [dt^3/3   0        dt^2/2   0;
         0        dt^3/3   0        dt^2/2;
         dt^2/2   0        dt       0;
         0        dt^2/2   0        dt];
```

### 4.3 模型间转移：Markov 链

模型之间的切换用转移概率矩阵 $\Pi$ 描述，$\pi_{ij} = P(\text{下帧为模型}\ j\ |\ \text{当前为模型}\ i)$：

$$\Pi = \begin{bmatrix} 0.95 & 0.05 \\ 0.10 & 0.90 \end{bmatrix}$$

**解读**：无人机大部分时间匀速飞行（95% 保持 CV），偶尔机动（5% 切换）；一旦进入机动状态，90% 概率继续机动。每行之和为 1。

### 4.4 IMM 三步骤

#### 步骤 A：交互混合（Mixing）

*目的：让每个模型的初始条件综合所有模型的历史估计，实现"软切换"。*

**预测模型概率**：

$$\bar{c}_j = \sum_{i=1}^{M} \pi_{ij} \mu_i \quad \text{（这是模型 j 的先验概率）}$$

**混合系数**：

$$\mu_{i|j} = \frac{\pi_{ij} \mu_i}{\bar{c}_j}$$

**混合初始状态和协方差**：

$$\bar{\mathbf{x}}_{0j} = \sum_{i=1}^{M} \mu_{i|j} \hat{\mathbf{x}}_i$$

$$\bar{P}_{0j} = \sum_{i=1}^{M} \mu_{i|j} \left[ P_i + (\hat{\mathbf{x}}_i - \bar{\mathbf{x}}_{0j})(\hat{\mathbf{x}}_i - \bar{\mathbf{x}}_{0j})^T \right]$$

**代数恒等式**（重要！）：当 $F_1 = F_2 = F$ 时，$\bar{c}_j$ 是融合预测的正确权重：

$$\hat{\mathbf{x}}(k|k-1) = \sum_j \bar{c}_j \hat{\mathbf{x}}_j(k|k-1) = F \hat{\mathbf{x}}(k-1|k-1)$$

**对应代码**（`imm_mix.m`）：

```matlab
c_bar = Pi' * mu;                           % c̄_j = Σ_i π_{ij} μ_i
mu_ij = Pi(:, j) .* mu ./ c_bar(j);        % 混合系数
x_mix = Σ μ_{i|j} * models(i).x;           % 混合状态
P_mix = Σ μ_{i|j} * (P_i + Δx Δx');        % 混合协方差（含离散项）
```

#### 步骤 B：各模型独立预测

对每个模型 $j$，用混合后的状态做线性预测（EKF 预测步骤）：

$$\hat{\mathbf{x}}_j(k|k-1) = F \bar{\mathbf{x}}_{0j}$$

$$P_j(k|k-1) = F \bar{P}_{0j} F^T + Q_j$$

**对应代码**（`imm_predict.m`）：

```matlab
models(j).x = F * models(j).x;        % 状态预测
P = F * models(j).P * F' + Q;         % 协方差预测
models(j).P = (P + P') / 2;           % 强制对称
```

#### 步骤 C：融合为全局预测（供门控使用）

$$\hat{\mathbf{x}}(k|k-1) = \sum_j \bar{c}_j \hat{\mathbf{x}}_j(k|k-1)$$

$$P(k|k-1) = \sum_j \bar{c}_j \left[ P_j(k|k-1) + \left(\hat{\mathbf{x}}_j - \hat{\mathbf{x}}\right)\left(\hat{\mathbf{x}}_j - \hat{\mathbf{x}}\right)^T \right]$$

**注意**：这里权重用 $\bar{c}_j$（预测模型概率），而非上帧的 $\mu_j$。这是理论上的正确选择（见作者答辩 B1 部分）。

**对应代码**（`main_imm_jpda_ekf.m`）：

```matlab
[tracks(i).models, c_bar_i] = imm_mix(tracks(i).models, tracks(i).mu, tracks(i).Pi);
tracks(i).c_bar_ = c_bar_i;
tracks(i).models = imm_predict(tracks(i).models);
[tracks(i).x, tracks(i).P] = imm_fuse(tracks(i).models, tracks(i).c_bar_);
```

---

## 5. Step 2：量测门控

### 5.1 为什么要门控？

若直接对所有探测做 JPDA，计算量随探测数指数增长。门控的作用是提前淘汰"不可能来自该目标"的探测，大幅减少后续计算。

### 5.2 马氏距离（Mahalanobis Distance）

对于轨迹 $t$ 和探测 $d$，定义**新息向量**（实际量测减去预测量测）：

$$\mathbf{v}_{t,d} = \mathbf{z}_d - H \hat{\mathbf{x}}_t(k|k-1)$$

定义**新息协方差**（综合了预测不确定性与量测噪声）：

$$S_{t,d} = H P_t(k|k-1) H^T + R_d$$

**马氏距离**（无量纲，考虑了不同方向的不确定性差异）：

$$d^2_{t,d} = \mathbf{v}_{t,d}^T S_{t,d}^{-1} \mathbf{v}_{t,d}$$

### 5.3 验证门（Validation Gate）

若 $d^2_{t,d} < \gamma$，则探测 $d$ 通过轨迹 $t$ 的验证门。

$\gamma$ 由 $\chi^2$ 分布确定：量测为 2 维，取 $\gamma = \chi^2(2,\, 99.9\%) = 13.816$（意味着真实量测落在门外的概率仅 0.1%）。

直觉理解：若把预测位置误差看作一个椭圆，马氏距离衡量探测点落在椭圆的"第几层"，门限限定了最大允许层数。

**输出**：验证矩阵（Validation Matrix）$\Omega \in \{0,1\}^{T \times D}$，$\Omega_{t,d}=1$ 表示探测 $d$ 通过轨迹 $t$ 的验证门。

**对应代码**（`jpda_gating.m`）：

```matlab
v = z - H * x_pred;          % 新息向量
S = H * P_pred * H' + R;     % 新息协方差
d2 = v' * S_inv * v;         % 马氏距离²
if d2 < cfg.gate_chi2        % 是否通过门控
    valid_mat(t, d) = true;
end
```

---

## 6. Step 3：JPDA 数据关联

这是整个系统的核心模块，也是计算量最大的部分。

### 6.1 为什么 JPDA 而不是简单匹配？

**最近邻法（Nearest Neighbor）** 的问题：强行把每个探测分配给最近的轨迹，当两条轨迹靠近时，一个探测被"抢走"，另一条轨迹完全失去更新，在交叉场景中经常发生 ID 混乱。

**JPDA 的思路**：不做硬决策，而是计算每个探测来自每个目标的**概率**，用概率加权来更新，处理不确定性。

### 6.2 关联概率的定义

定义边缘关联概率：

$$\beta_{t,d} = P(\text{探测}\ d\ \text{来自目标}\ t\ |\ \text{所有量测})$$

$$\beta_{t,0} = P(\text{目标}\ t\ \text{本帧未被检测}\ |\ \text{所有量测})$$

满足约束：$\beta_{t,0} + \sum_d \beta_{t,d} = 1 \quad \forall t$

### 6.3 JPDA 四步计算

#### 步骤 1：聚类（Clustering）

*目的：将互不相干的目标-探测组解耦，分别处理，降低复杂度。*

在目标-探测二部图（bipartite graph）上：若目标 $t_1$ 和探测 $d$ 通过验证门（$\Omega_{t_1,d}=1$），连一条边。若目标 $t_2$ 也与 $d$ 相连，则 $t_1$ 和 $t_2$ 在同一个簇中。

用 BFS（广度优先搜索）找出所有连通分量，各簇相互独立处理。

**对应代码**（`jpda_cluster.m`）：

```matlab
% BFS 建立连通分量
while head <= length(q)
    t = q(head); head = head + 1;
    for d = find(valid_mat(t, :))     % 找轨迹 t 的所有门内探测
        for t2 = find(valid_mat(:, d))'  % 找共享探测 d 的其他轨迹
            if t_label(t2) == 0
                t_label(t2) = cid;
                q(end+1) = t2;
            end
        end
    end
end
```

#### 步骤 2：枚举联合事件（Joint Events）

*目的：列出所有可能的关联方案，每种方案称为一个"联合事件"。*

对一个簇内有 $T_{\text{loc}}$ 个目标和 $D_{\text{loc}}$ 个探测，一个联合事件 $\theta$ 是一个长度为 $T_{\text{loc}}$ 的向量：

$$\theta = [\theta(1),\ \theta(2),\ \ldots,\ \theta(T_{\text{loc}})]$$

- $\theta(t) = 0$：目标 $t$ 本帧未被检测
- $\theta(t) = d$：目标 $t$ 与探测 $d$ 关联

**可行性约束**（这是 JPDA 的关键约束）：
1. 若 $\theta(t) = d \neq 0$，则 $\Omega_{t,d} = 1$（必须通过验证门）
2. 若 $\theta(t_1) = \theta(t_2) = d \neq 0$，则 $t_1 = t_2$（每个探测最多被一个目标关联）

枚举使用 DFS 递归，`used_flag` 剪枝（已占用的探测不再分配）：

**对应代码**（`jpda_enumerate.m`）：

```matlab
function recurse(t, theta, used_flag)
    % 选项 A：目标 t 本帧漏检
    theta_new(t) = 0;
    recurse(t+1, theta_new, used_flag);
    
    % 选项 B：目标 t 与探测 d 关联
    for d = 1:D_loc
        if valid_loc(t,d) && ~used_flag(d)  % 门内 且 未被占用
            theta_new(t) = d;
            used_new(d) = true;
            recurse(t+1, theta_new, used_new);
        end
    end
end
```

#### 步骤 3：计算事件概率

对于每个联合事件 $\theta$，其**未归一化对数概率**（Poisson 杂波模型，Bar-Shalom §6.2.2）：

$$\log P(\theta) \propto \underbrace{\sum_{t:\,\theta(t)\neq 0} \left[ \log P_d + \log \mathcal{L}_{t,\theta(t)} - \log \lambda_c \right]}_{\text{被检测目标的贡献}} + \underbrace{\sum_{t:\,\theta(t)=0} \log(1 - P_d)}_{\text{漏检目标的贡献}}$$

其中：
- $P_d = 0.90$：检测概率（每帧有 90% 概率被探测到）
- $\lambda_c = 10^{-4}\ \text{m}^{-2}$：杂波空间密度（单位面积平均杂波探测数）
- $\mathcal{L}_{t,d} = \mathcal{N}(\mathbf{v}_{t,d};\ \mathbf{0},\ S_{t,d})$：高斯量测似然

**直觉解释**：
- 一个探测"属于目标"的概率正比于 $P_d \cdot \mathcal{L}_{t,d}$（检测到，且与预测吻合）
- 一个探测"属于杂波"的概率正比于 $\lambda_c$（背景噪声密度）
- 目标"被漏检"的概率正比于 $1-P_d$

数值归一化（避免下溢）：

```matlab
log_P = log_P - max(log_P);    % 减去最大值（log 域稳定化）
P_evt = exp(log_P);
P_evt = P_evt / sum(P_evt);    % 归一化为概率
```

#### 步骤 4：边缘化得到 β

$$\beta_{t,d} = \sum_{\theta:\,\theta(t)=d} P(\theta) \qquad \beta_{t,0} = \sum_{\theta:\,\theta(t)=0} P(\theta)$$

**对应代码**（`jpda_compute_beta.m`）：

```matlab
for e = 1:n_events
    theta = Theta{e};
    pw    = P_evt(e);
    for ti = 1:Tloc
        t = t_idx(ti);
        if theta(ti) == 0
            beta0(t) = beta0(t) + pw;
        else
            d = d_idx(theta(ti));
            beta(t, d) = beta(t, d) + pw;
        end
    end
end
```

---

## 7. Step 4：PDA 状态更新

### 7.1 更新思路

JPDA 给出了每个探测来自每个目标的概率 $\beta_{t,d}$。现在用这些概率对状态进行**加权更新**：不是只用一个探测，而是用所有门内探测的概率加权平均来更新。

### 7.2 IMM 框架下的更新：对每个模型分别进行

对模型 $j$ 的预测状态 $\hat{\mathbf{x}}_j(k|k-1)$ 和协方差 $P_j(k|k-1)$：

**Step A：计算各门内探测的新息**

$$\mathbf{v}_d = \mathbf{z}_d - H \hat{\mathbf{x}}_j(k|k-1) \quad \forall d \in \text{门内探测}$$

**Step B：加权 R 与卡尔曼增益**（答辩 **B2**，两种写法需实验区分）

| 写法 | 公式 | 代码开关 |
|------|------|----------|
| **A（当前默认）** | $\bar{R} = \sum_d \beta_d R_d$ | `cfg.pda_Rbar_normalized = false` |
| **B（说明书/条件检测）** | $\bar{R} = \dfrac{\sum_d \beta_d R_d}{1-\beta_0}$ | `cfg.pda_Rbar_normalized = true` |

当 $\beta_0 \approx 0$ 时 $1-\beta_0 \approx 1$，**A 与 B 数值几乎相同**。

**注意（仿真易混淆）**：

| 量 | 含义 | 典型范围 |
|----|------|----------|
| 传感器 $P_d$ | 仿真里本帧是否生成真值量测 | $0.85\sim0.95$ |
| JPDA $\beta_0$ | **已有门内探测**时，目标“未被解释”的边际概率 | 有量测时 often $0.01\sim0.12$ |
| 无探测帧 | $\beta_0=1$ 但 $D_v=0$，**不经过** $\bar R$ 更新 | — |

压低传感器 $P_d$ 只会增加“无量测帧”，**不能**代替“大 $\beta_0$ 的 B2 实验”。B2 对照应使用：**实验1 固定 $\beta$**，或 **仅杂波入门 / 低 $P_{d,\mathrm{cfg}}$ 单帧 JPDA 快照**（`check_b2_Rbar.m`）。$\beta_0\in[0.25,0.80]$ 时 $\bar R_B/\bar R_A = 1/(1-\beta_0)$ 方可显著区分。

$$\bar{S} = H P_j H^T + \bar{R}, \qquad K_j = P_j H^T \bar{S}^{-1}$$

**Step C：合成新息**

$$\bar{\mathbf{v}} = \sum_d \beta_d \mathbf{v}_d$$

**Step D：状态更新**

$$\hat{\mathbf{x}}_j(+) = \hat{\mathbf{x}}_j(k|k-1) + K_j \bar{\mathbf{v}}$$

**Step E：协方差更新（完整三项 PDA 公式，Bar-Shalom §6.3.2）**

$$\boxed{P_j(+) = \underbrace{\beta_0 P_j(-)}_{\text{项1：漏检分量}} + \underbrace{(1-\beta_0) P_c}_{\text{项2：条件协方差}} + \underbrace{K_j \left[\sum_d \beta_d \mathbf{v}_d \mathbf{v}_d^T - \bar{\mathbf{v}} \bar{\mathbf{v}}^T\right] K_j^T}_{\text{项3：innovation spread（不可省略）}}}$$

其中 $P_c = P_j(-) - K_j \bar{S} K_j^T$（若已确认来自某探测时的条件协方差）。

### 7.3 三项公式的物理意义

```
P_j(+) = β₀·P(-)     漏检：没有量测，不确定性维持不减
       + (1-β₀)·P_c   有效检测：量测带来信息增益，不确定性减小
       + K[...]K'      来源不确定性：同时有多个候选探测，
                       不知道用哪个更新，额外引入不确定性
```

**第三项最容易被遗漏**：当周围没有其他目标（只有一个探测 $\beta_1 \approx 1$）时，$\beta_1 \mathbf{v}_1\mathbf{v}_1^T - \bar{\mathbf{v}}\bar{\mathbf{v}}^T \approx \mathbf{v}\mathbf{v}^T - \mathbf{v}\mathbf{v}^T = 0$，第三项自然消失，退化为标准 KF。当多目标密集交叉时，第三项显著增大，防止滤波器在混乱场景下过度自信。

**对应代码**（`compute_R_bar.m` + `ekf_pda_update.m`）：

```matlab
R_bar = compute_R_bar(beta_valid, R_valid, beta0, cfg.pda_Rbar_normalized);
% 项 1：漏检分量
P_term1 = beta0 * P_pred;

% 项 2：条件协方差
P_c     = P_pred - K * S_bar * K';
P_term2 = (1 - beta0) * P_c;

% 项 3：innovation spread（不可省略！）
S_spread = zeros(m, m);
for d = 1:Dv
    S_spread = S_spread + beta_valid(d) * (V(:,d) * V(:,d)');
end
P_term3 = K * (S_spread - v_bar * v_bar') * K';

P_upd = P_term1 + P_term2 + P_term3;
```

### 7.4 IMM 模型概率更新

每个模型的似然（对量测的解释能力）：

$$\Lambda_j \approx \lambda_c + P_d \sum_d \beta_d \mathcal{N}(\mathbf{v}_d;\ \mathbf{0},\ \bar{S}_j)$$

更新模型概率（贝叶斯后验，Bar-Shalom §11.6.6 eq.11.6.6-8）：

$$\mu_j(k|k) = \frac{\Lambda_j \cdot \bar{c}_j}{\sum_{j'} \Lambda_{j'} \cdot \bar{c}_{j'}}$$

**直觉**：若模型 j 预测的位置与实际量测吻合（$\Lambda_j$ 大），则其概率上升；若预测偏差大（$\Lambda_j$ 小），则其概率下降。

### 7.5 IMM 最终融合输出

$$\hat{\mathbf{x}}(k|k) = \sum_j \mu_j(k|k) \hat{\mathbf{x}}_j(k|k)$$

$$P(k|k) = \sum_j \mu_j(k|k) \left[ P_j(k|k) + \left(\hat{\mathbf{x}}_j - \hat{\mathbf{x}}\right)\left(\hat{\mathbf{x}}_j - \hat{\mathbf{x}}\right)^T \right]$$

**对应代码**（`track_imm_pda_update.m`）：

```matlab
for j = 1:M
    [x_upd, P_upd, Lj] = ekf_pda_update(x_j, P_j, H, Z_valid, ...
                          R_valid, beta_valid, beta0_val, cfg.lambda_c);
    track.models(j).x = x_upd;
    track.models(j).P = P_upd;
    Lambda(j) = Lj;
end
track.mu = imm_update_mu(track.mu, Lambda, c_bar);
[track.x, track.P] = imm_fuse(track.models, track.mu);
```

---

## 8. Step 5~7：轨迹管理

### 8.1 轨迹的三种状态

每条轨迹（Track）有一个**状态机**，在三个状态之间转移：

```
         M次命中（在最近N帧中）
Tentative ──────────────────────→ Confirmed
（暂定）                           （已确认）
    │                                │
    │ 连续漏检 ≥ 2 帧                │ 连续漏检 ≥ 5 帧
    ↓                                ↓
  Deleted  ←──────────────────── Deleted
（已删除）                         （已删除）
```

### 8.2 M/N 确认逻辑（Blackman §3.3）

系统维护一个长度为 $N$ 的**滑动历史窗口**：

$$\text{history} = [h_1,\ h_2,\ \ldots,\ h_N], \quad h_i \in \{0, 1\}$$

其中 $h_i=1$ 表示该帧有量测进入该轨迹的验证门（命中），$h_i=0$ 表示漏检。

**确认条件**（默认 $M=3$，$N=5$，即"5 帧中至少 3 次命中"）：

$$\sum_{i=1}^{N} h_i \geq M \Rightarrow \text{Tentative} \to \text{Confirmed}$$

**为什么需要这个机制？** 杂波产生的假轨迹通常无法连续命中，在 $N$ 帧后自然消亡；真实目标由于 $P_d = 90\%$，在 5 帧内 3 次命中的概率很高（$>99.9\%$）。

### 8.3 轨迹诞生（Track Birth）

每帧结束后，统计哪些探测**不在任何现有轨迹的验证门内**（未关联探测）。对每个未关联探测，创建一条新的 Tentative 轨迹：

$$\hat{\mathbf{x}}_0 = \begin{bmatrix} z_x \\ z_y \\ 0 \\ 0 \end{bmatrix}, \quad P_0 = \text{diag}([\sigma_{z}^2 + \sigma_{p_0}^2,\ \sigma_{z}^2 + \sigma_{p_0}^2,\ \sigma_{v_0}^2,\ \sigma_{v_0}^2])$$

初始速度设为零（未知），位置不确定性综合量测噪声与先验。

**对应代码**（`track_manage.m`，`track_create.m`）：

```matlab
% 找出未关联探测：不在任何轨迹门内
unassigned_dets = find(~any(assoc.valid_mat, 1));

for d = unassigned_dets
    new_track = track_create(Z(:,d), R_list(:,:,d), next_id, frame_idx, cfg);
    tracks(end+1) = new_track;
    next_id = next_id + 1;
end
```

### 8.4 完整生命周期示例

```
帧号:    1    2    3    4    5    6    7    ...   12   13   14
命中:    ●    ●    ○    ●    ●    ●    ●    ...   ○    ○    ○
状态: [T] [T] [T] [T] [C] [C] [C]    ...  [C]  [C]  [X]
                              ↑                        ↑
                         3/5 确认                 连续5次漏检删除

● = 命中, ○ = 漏检, T = Tentative, C = Confirmed, X = Deleted
```

---

## 9. 参数选择与调参指南

### 9.1 参数汇总

| 参数 | 默认值 | 物理含义 | 调大效果 | 调小效果 |
|---|---|---|---|---|
| `cv_q` | 0.5 m²/s³ | CV 模型过程噪声 | 允许更大加速度 | 更"相信"匀速 |
| `ca_q` | 8.0 m²/s³ | 高机动模型过程噪声 | 模型更"松弛" | 机动响应更慢 |
| `gate_chi2` | 13.816 | 验证门 χ² 门限 | 门更宽，更多探测进门 | 门更窄，可能丢真实探测 |
| `Pd` | 0.90 | 检测概率 | β₀ 降低 | 对漏检更宽容 |
| `lambda_c` | 1e-4 /m² | 杂波密度 | 对杂波更警惕 | 更相信探测是真实目标 |
| `mn_M` | 3 | 确认命中次数 | 确认更严格 | 更快确认（也更多假轨迹） |
| `mn_N` | 5 | 确认窗口长度 | 决策更慢 | 决策更快 |
| `del_tentative` | 2 | Tentative 删除阈值 | 更宽容（假轨迹更多） | 更激进删除 |
| `del_confirmed` | 5 | Confirmed 删除阈值 | 允许更长遮挡 | 目标遮挡时更早删除 |

### 9.2 传感器参数的物理对应

| 参数 | 数值 | 对应传感器 | 物理意义 |
|---|---|---|---|
| `R_radar` | diag([1², 1²]) m² | 雷达 | 位置标准差约 1 m |
| `R_acoustic` | diag([8², 8²]) m² | 声学 | 位置标准差约 8 m |
| `P0_pos` | 50² = 2500 m² | 初始轨迹 | 位置初始不确定性 50 m |
| `P0_vel` | 20² = 400 m²/s² | 初始轨迹 | 速度初始不确定性 20 m/s |

---

## 10. 公式索引与代码对照表

| 公式 | 数学表达 | 代码文件 | 行数区间 |
|---|---|---|---|
| 预测模型概率 | $\bar{c}_j = \Pi^T \boldsymbol{\mu}$ | `imm_mix.m` | L34 |
| 混合系数 | $\mu_{i\|j} = \pi_{ij}\mu_i/\bar{c}_j$ | `imm_mix.m` | L39 |
| 混合状态 | $\bar{x}_{0j} = \sum_i \mu_{i\|j} \hat{x}_i$ | `imm_mix.m` | L43-46 |
| 混合协方差 | $\bar{P}_{0j} = \sum_i \mu_{i\|j}(P_i + \Delta x_i \Delta x_i^T)$ | `imm_mix.m` | L49-53 |
| EKF 状态预测 | $\hat{x}_j^- = F\bar{x}_{0j}$ | `imm_predict.m` | L14 |
| EKF 协方差预测 | $P_j^- = FP_{0j}F^T + Q_j$ | `imm_predict.m` | L15 |
| IMM 融合状态 | $\hat{x} = \sum_j \mu_j \hat{x}_j$ | `imm_fuse.m` | L22-24 |
| IMM 融合协方差 | $P = \sum_j \mu_j(P_j + \Delta x_j \Delta x_j^T)$ | `imm_fuse.m` | L26-30 |
| 马氏距离 | $d^2 = v^T S^{-1} v$ | `jpda_gating.m` | L37-40 |
| 新息协方差 | $S = HPH^T + R$ | `jpda_gating.m` | L35-36 |
| 联合事件概率 | $\log P(\theta) \propto \sum_{\text{det}} [\log P_d + \log \mathcal{L} - \log\lambda_c] + \sum_{\text{miss}} \log(1-P_d)$ | `jpda_compute_beta.m` | L85-94 |
| β 边缘化 | $\beta_{t,d} = \sum_{\theta:\theta(t)=d} P(\theta)$ | `jpda_compute_beta.m` | L102-114 |
| 合成新息 | $\bar{v} = \sum_d \beta_d v_d$ | `ekf_pda_update.m` | L98 |
| PDA 状态更新 | $\hat{x}^+ = \hat{x}^- + K\bar{v}$ | `ekf_pda_update.m` | L101 |
| PDA 协方差项1 | $\beta_0 P^-$ | `ekf_pda_update.m` | L106 |
| PDA 协方差项2 | $(1-\beta_0)P_c$ | `ekf_pda_update.m` | L109-111 |
| PDA 协方差项3 | $K[\sum\beta_d v_d v_d^T - \bar{v}\bar{v}^T]K^T$ | `ekf_pda_update.m` | L113-118 |
| 模型概率更新 | $\mu_j^+ = \Lambda_j \bar{c}_j / \sum_k \Lambda_k \bar{c}_k$ | `imm_update_mu.m` | L23-29 |
| M/N 确认逻辑 | $\sum h_i \geq M$ in last $N$ | `track_manage.m` | L71-78 |

---

## 附录：符号表

| 符号 | 含义 | 维度 |
|---|---|---|
| $\mathbf{x}$ | 目标状态向量 $[p_x, p_y, v_x, v_y]^T$ | 4×1 |
| $P$ | 状态协方差矩阵 | 4×4 |
| $\mathbf{z}$ | 量测向量 $[z_x, z_y]^T$ | 2×1 |
| $H$ | 量测矩阵 | 2×4 |
| $R$ | 量测噪声协方差 | 2×2 |
| $F$ | 状态转移矩阵 | 4×4 |
| $Q$ | 过程噪声协方差 | 4×4 |
| $K$ | 卡尔曼增益 | 4×2 |
| $\mathbf{v}$ | 新息向量 $\mathbf{z} - H\hat{\mathbf{x}}$ | 2×1 |
| $S$ | 新息协方差 $HPH^T+R$ | 2×2 |
| $\mu_j$ | 模型 $j$ 的后验概率 | 标量 |
| $\bar{c}_j$ | 模型 $j$ 的预测概率 | 标量 |
| $\Pi$ | Markov 转移矩阵，$\Pi_{ij}=P(j\|i)$ | $M\times M$ |
| $\beta_{t,d}$ | 探测 $d$ 来自目标 $t$ 的边缘概率 | 标量 |
| $\beta_{t,0}$ | 目标 $t$ 本帧漏检概率 | 标量 |
| $\Omega_{t,d}$ | 验证矩阵，1 表示通过门控 | 布尔 |
| $P_d$ | 检测概率 | 标量 |
| $\lambda_c$ | 杂波空间密度 | m⁻² |
| $\gamma$ | 验证门门限（χ²分位数） | 标量 |

---

*文档版本：v1.0 | 2026-06-03*  
*对应代码：`DroneSimulation/Tracker/`*  
*参考：Bar-Shalom (2001) Ch.6, Ch.11 | Blackman & Popoli (1999) Ch.3*
